#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
RECALL_TARGET_CMD="${RECALL_CLAUDE_BIN:-claude}" \
RECALL_CLIENT_NAME="claude" \
exec "${script_dir}/recall-session" "$@"
